import reducers from './reducers';

// Named exports for types, selectors, and operations
export { default as sectionsTypes } from './types';
export { default as sectionsSelectors } from './selectors';
export { default as sectionsOperations } from './operations';

// Default export is the reducers
export default reducers;
